# Archived AppleDouble Metadata Files

These files are no longer active website documentation.

They are preserved only because they existed in active documentation paths and looked like duplicate Markdown docs. They are AppleDouble/resource-fork metadata files, not rule sources. Use the active docs linked from `../../../README.md` instead.
